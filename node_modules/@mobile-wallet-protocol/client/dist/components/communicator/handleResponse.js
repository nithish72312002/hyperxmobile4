"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.handleResponse = handleResponse;
const Communicator_1 = require("./webBased/Communicator");
const constants_1 = require("../../core/constants");
/**
 * Handles the response from a deeplink.
 *
 * @param responseUrl - The URL of the response.
 * @returns A boolean indicating whether the response was handled successfully.
 */
function handleResponse(responseUrl) {
    const pathname = new URL(responseUrl).pathname;
    if (!pathname.includes(constants_1.MWP_RESPONSE_PATH)) {
        return false;
    }
    if (Communicator_1.WebBasedWalletCommunicator.handleResponse(responseUrl)) {
        return true;
    }
    // NativeWalletCommunicator.handleResponse
    return false;
}
