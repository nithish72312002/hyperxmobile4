"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.postRequestToWallet = postRequestToWallet;
const Communicator_1 = require("./webBased/Communicator");
/**
 * Posts a request to a wallet and waits for the response.
 *
 * @param request - The request to send.
 * @param wallet - The wallet to send the request to.
 * @returns A promise that resolves to the response.
 */
async function postRequestToWallet(request, wallet) {
    const { type, scheme } = wallet;
    if (type === 'webBased') {
        return Communicator_1.WebBasedWalletCommunicator.postRequestAndWaitForResponse(request, scheme);
    }
    if (type === 'native') {
        throw new Error('Native wallet not supported yet');
    }
    throw new Error('Unsupported wallet type');
}
